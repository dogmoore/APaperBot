module.exports = {
    name: 'restart',
    discription: 'restarts the bot',
    execute(message) {
        const Discord = require('discord.js');
        message.reply('I SHALL NEVER BE KILLED!');
        
    }
}